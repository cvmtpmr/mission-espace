"use client";

export default function SolarSystem() {
  return (
    <div
      aria-hidden="true"
      style={{
        position: "fixed",
        inset: 0,
        background: "black",
        zIndex: 0,
        pointerEvents: "none",
      }}
    />
  );
}

